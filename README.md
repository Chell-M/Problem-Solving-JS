the script.js is from previous section(array and callbacks) 
the script2.js is the most current section, exactly the same as previous project but instead includes arrow functions and implicit returns
